spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/wrm_test_db"
    username: "postgres"
    password: "@@root@"
    driver-class-name: org.postgresql.Driver
  web:
    resources:
      add-mappings: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

app:
  default:
    algorithm: FIFO